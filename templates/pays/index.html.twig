{% extends 'base.html.twig' %}

{% block title %}Pays index{% endblock %}

{% block body %}
    <div class="container">
        <div class="row">
            <div class="col-md-2"></div>
            <div class="col-md-8 text-dark bg-light rounded" style="max-height: 60vh;overflow-y: auto">
    <h1>Pays index     <a href="{{ path('pays_new') }}" class="btn btn-primary mb-2">Create new</a>
    </h1>

    <table class="table">
        <thead>
            <tr>
                <th>Name</th>
                <th>Capitale</th>
                <th>Forme_courte</th>
                <th>Forme_longue</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for pay in pays %}
            <tr>
                <td>{{ pay.name }}</td>
                <td>{{ pay.capitale }}</td>
                <td>{{ pay.formeCourte }}</td>
                <td>{{ pay.formeLongue }}</td>
                <td>
                    <!--a href="{{ path('pays_show', {'id': pay.id}) }}" class="btn btn-info">voir</a-->
                    <a href="{{ path('pays_edit', {'id': pay.id}) }}" class="btn btn-primary">modifier</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="7">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

            </div>
            <div class="col-md-2"></div>
        </div>
    </div>

{% endblock %}
