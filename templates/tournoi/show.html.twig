
{% block body %}

    {% include 'head.html.twig' %}

    <h1>Tournoi</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ tournoi.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ tournoi.name }}</td>
            </tr>
            <tr>
                <th>Discipline</th>
                <td>{{ tournoi.discipline }}</td>
            </tr>
            <tr>
                <th>Association</th>
                <td>{{ tournoi.genre }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('tournoi_index') }}" class="btn btn-primary">retour</a>

    <a href="{{ path('tournoi_edit', {'id': tournoi.id}) }}" class="btn btn-primary">modifier</a>

    {{ include('tournoi/_delete_form.html.twig') }}


    {% include 'end.html.twig' %}
{% endblock %}
