
{% block body %}
    {{ include('head.html.twig') }}

    <h1>modifier l'Affiche</h1>

    {{ include('affiche/_form.html.twig', {'button_label': 'modifier'}) }}

    <a href="{{ path('affiche_index') }}">retour</a>

    {{ include('affiche/_delete_form.html.twig') }}

    {{ include('end.html.twig') }}

{% endblock %}
