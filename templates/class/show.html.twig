{% extends 'base.html.twig' %}

{% block title %}Classement{% endblock %}

{% block body %}
    <h1>Classement</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ classement.id }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ classement.type }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('class_index') }}">back to list</a>

    <a href="{{ path('class_edit', {'id': classement.id}) }}">edit</a>

    {{ include('class/_delete_form.html.twig') }}
{% endblock %}
