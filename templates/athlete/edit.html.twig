
{% block body %}
    {{ include('head.html.twig') }}

    <h1>Edit Athlete</h1>

    {{ include('athlete/_form.html.twig', {'button_label': 'modifier'}) }}

    <a href="{{ path('athlete_index') }}" class="btn btn-primary">retour</a>

    {{ include('athlete/_delete_form.html.twig') }}
    {{ include('end.html.twig') }}

{% endblock %}
